# フォルダ機能 + Vaultスイッチャー 実装計画

<!-- derived-from ../specs/2026-07-02-folders-design.md -->

対象仕様: [../specs/2026-07-02-folders-design.md](../specs/2026-07-02-folders-design.md)

## 実装原則

- DB上の `NULL` はVault直下の未分類メモを表す。UIとGETクエリだけ `folderId=root` を使う。
- APIは `memos.folder_id` と `memos.vault_id` のVault一致を必ず検証する。
- APIは `folders.parent_id` のVault一致とサイクル禁止を必ず検証する。
- APIは子フォルダまたはメモを持つフォルダの削除に409を返す。
- 画面はフォルダツリー、メモ一覧、エディタの3ペインを維持する。Vault一覧の常駐表示はVaultスイッチャーへ移す。

## Task 01: Vitest基盤を追加する

**Goal**

APIはD1込み、Webはjsdom込み、sharedは型を実行時サンプルで確認できる状態にする。

**Files**

- `package.json`
- `package-lock.json`
- `turbo.json`
- `apps/api/package.json`
- `apps/api/vitest.config.ts`
- `apps/api/src/test/db.ts`
- `apps/api/src/index.test.ts`
- `apps/web/package.json`
- `apps/web/vite.config.ts`
- `apps/web/src/setupTests.ts`
- `apps/web/src/test/factories.ts`
- `apps/web/src/test/mockApi.ts`
- `apps/web/src/components/MemoList.test.tsx`
- `packages/shared/package.json`
- `packages/shared/vitest.config.ts`
- `packages/shared/src/index.test.ts`

**Test**

```ts
// apps/api/src/index.test.ts
import { env } from "cloudflare:test";
import { describe, expect, test } from "vitest";
import "./test/db";
import app from "./index";

describe("api", () => {
  test("GET /api/vaults returns an empty list", async () => {
    const res = await app.request("/api/vaults", {}, env);
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual([]);
  });
});
```

```tsx
// apps/web/src/components/MemoList.test.tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, test, vi } from "vitest";
import { MemoList } from "./MemoList";

describe("MemoList", () => {
  test("renders the empty state", () => {
    render(<MemoList memos={[]} selectedId={null} onSelect={vi.fn()} />);
    expect(screen.getByText("メモがありません")).toBeInTheDocument();
  });
});
```

**Implementation**

```jsonc
// package.json
{
  "scripts": {
    "test": "turbo test"
  }
}
```

```jsonc
// turbo.json
{
  "tasks": {
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
```

```ts
// apps/api/vitest.config.ts
import {
  defineWorkersConfig,
  readD1Migrations,
} from "@cloudflare/vitest-pool-workers/config";

export default defineWorkersConfig(async () => {
  const migrations = await readD1Migrations("./migrations");
  return {
    test: {
      poolOptions: {
        workers: {
          wrangler: { configPath: "./wrangler.jsonc" },
          miniflare: { bindings: { TEST_MIGRATIONS: migrations } },
        },
      },
    },
  };
});
```

```ts
// apps/api/src/test/db.ts
import { applyD1Migrations, env } from "cloudflare:test";
import { beforeAll, beforeEach } from "vitest";
import type { Env } from "../index";

declare module "cloudflare:test" {
  interface ProvidedEnv extends Env {
    TEST_MIGRATIONS: D1Migration[];
  }
}

beforeAll(async () => {
  await applyD1Migrations(env.DB, env.TEST_MIGRATIONS);
});

beforeEach(async () => {
  await env.DB.batch([
    env.DB.prepare("DELETE FROM memos"),
    env.DB.prepare("DELETE FROM vaults"),
  ]);
});
```

```ts
// apps/web/vite.config.ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: { "@": path.resolve(__dirname, "./src") },
  },
  server: {
    proxy: { "/api": "http://localhost:8787" },
  },
  test: {
    environment: "jsdom",
    setupFiles: "./src/setupTests.ts",
  },
});
```

```ts
// apps/web/src/setupTests.ts
import "@testing-library/jest-dom/vitest";
```

```ts
// apps/web/src/test/factories.ts
export const now = "2026-07-02T00:00:00.000Z";

export function vault(id: string, name: string, memoCount: number) {
  return { id, name, memoCount, createdAt: now, updatedAt: now };
}

export function folder(input: {
  id: string;
  vaultId?: string;
  parentId?: string | null;
  name: string;
  memoCount?: number;
}) {
  return {
    id: input.id,
    vaultId: input.vaultId ?? "v1",
    parentId: input.parentId ?? null,
    name: input.name,
    memoCount: input.memoCount ?? 0,
    createdAt: now,
    updatedAt: now,
  };
}

export function memo(input: { id: string; vaultId: string; folderId: string | null }) {
  return {
    id: input.id,
    vaultId: input.vaultId,
    folderId: input.folderId,
    title: "",
    content: "",
    createdAt: now,
    updatedAt: now,
  };
}

export function node(input: { id: string; name: string; totalMemoCount: number }) {
  const base = folder({ id: input.id, name: input.name, memoCount: input.totalMemoCount });
  return {
    id: base.id,
    vaultId: base.vaultId,
    parentId: base.parentId,
    name: base.name,
    createdAt: base.createdAt,
    updatedAt: base.updatedAt,
    memoCount: base.memoCount,
    children: [],
    depth: 0,
    totalMemoCount: input.totalMemoCount,
  };
}
```

```ts
// apps/web/src/test/mockApi.ts
import { vi } from "vitest";

export const mockApi = {
  vaults: { list: vi.fn(), create: vi.fn(), update: vi.fn(), delete: vi.fn() },
  folders: { list: vi.fn(), create: vi.fn(), update: vi.fn(), delete: vi.fn() },
  memos: { list: vi.fn(), create: vi.fn(), update: vi.fn(), delete: vi.fn() },
};
```

**Verification**

```bash
npm install -D vitest @cloudflare/vitest-pool-workers @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
npm test
npm run typecheck
```

## Task 02: foldersテーブルとmemos.folder_idを追加する

**Goal**

D1スキーマへ任意深さのフォルダ木を追加し、既存メモは `folder_id=NULL` のまま残す。

**Files**

- `apps/api/src/db/schema.ts`
- `apps/api/src/db/schema.test.ts`
- `apps/api/src/test/db.ts`
- `apps/api/migrations/0001_folders.sql`
- `apps/api/migrations/meta/0001_snapshot.json`
- `apps/api/migrations/meta/_journal.json`

**Test**

```ts
// apps/api/src/db/schema.test.ts
import { env } from "cloudflare:test";
import { describe, expect, test } from "vitest";
import "../test/db";

describe("folders schema", () => {
  test("stores a child folder and a memo assigned to it", async () => {
    await env.DB.prepare(
      "INSERT INTO vaults (id, name, created_at, updated_at) VALUES (?, ?, ?, ?)"
    ).bind("v1", "日々の記録", "2026-07-02T00:00:00.000Z", "2026-07-02T00:00:00.000Z").run();
    await env.DB.prepare(
      "INSERT INTO folders (id, vault_id, parent_id, name, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?)"
    ).bind("f1", "v1", null, "技術メモ", "2026-07-02T00:00:00.000Z", "2026-07-02T00:00:00.000Z").run();
    await env.DB.prepare(
      "INSERT INTO memos (id, vault_id, folder_id, title, content, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?)"
    ).bind("m1", "v1", "f1", "D1", "", "2026-07-02T00:00:00.000Z", "2026-07-02T00:00:00.000Z").run();

    const row = await env.DB.prepare("SELECT folder_id FROM memos WHERE id = ?").bind("m1").first<{ folder_id: string }>();
    expect(row?.folder_id).toBe("f1");
  });
});
```

**Implementation**

```ts
// apps/api/src/db/schema.ts
import {
  sqliteTable,
  text,
  type AnySQLiteColumn,
} from "drizzle-orm/sqlite-core";

export const folders = sqliteTable("folders", {
  id: text("id").primaryKey(),
  vaultId: text("vault_id").notNull().references(() => vaults.id),
  parentId: text("parent_id").references((): AnySQLiteColumn => folders.id),
  name: text("name").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
});

export const memos = sqliteTable("memos", {
  id: text("id").primaryKey(),
  vaultId: text("vault_id").notNull().references(() => vaults.id),
  folderId: text("folder_id").references(() => folders.id),
  title: text("title").notNull(),
  content: text("content").notNull(),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
});
```

```sql
-- apps/api/migrations/0001_folders.sql
CREATE TABLE `folders` (
  `id` text PRIMARY KEY NOT NULL,
  `vault_id` text NOT NULL,
  `parent_id` text,
  `name` text NOT NULL,
  `created_at` text NOT NULL,
  `updated_at` text NOT NULL,
  FOREIGN KEY (`vault_id`) REFERENCES `vaults`(`id`) ON UPDATE no action ON DELETE no action,
  FOREIGN KEY (`parent_id`) REFERENCES `folders`(`id`) ON UPDATE no action ON DELETE no action
);
--> statement-breakpoint
ALTER TABLE `memos` ADD `folder_id` text REFERENCES `folders`(`id`);
```

```ts
// apps/api/src/test/db.ts
beforeEach(async () => {
  await env.DB.batch([
    env.DB.prepare("DELETE FROM memos"),
    env.DB.prepare("DELETE FROM folders"),
    env.DB.prepare("DELETE FROM vaults"),
  ]);
});
```

**Verification**

```bash
npm run db:generate --workspace @memo/api
npm test --workspace @memo/api
npm run db:migrate:local --workspace @memo/api
npm run typecheck --workspace @memo/api
```

## Task 03: shared型へFolderとfolderIdを追加する

**Goal**

APIとWebが同じ `FolderWithCount` と `folderId` 入力型を使う。

**Files**

- `packages/shared/src/index.ts`
- `packages/shared/src/index.test.ts`

**Test**

```ts
// packages/shared/src/index.test.ts
import { describe, expect, expectTypeOf, test } from "vitest";
import type { CreateMemoInput, FolderSelection, FolderWithCount } from "./index";

describe("shared folder types", () => {
  test("accepts root selection and nullable memo folder input", () => {
    const selection: FolderSelection = "root";
    const folder = {
      id: "f1",
      vaultId: "v1",
      parentId: null,
      name: "技術メモ",
      memoCount: 0,
      createdAt: "2026-07-02T00:00:00.000Z",
      updatedAt: "2026-07-02T00:00:00.000Z",
    } satisfies FolderWithCount;
    const input = { vaultId: "v1", folderId: null, title: "", content: "" } satisfies CreateMemoInput;

    expect(selection).toBe("root");
    expect(folder.parentId).toBeNull();
    expectTypeOf(input.folderId).toEqualTypeOf<string | null | undefined>();
  });
});
```

**Implementation**

```ts
// packages/shared/src/index.ts
export type Folder = {
  id: string;
  vaultId: string;
  parentId: string | null;
  name: string;
  createdAt: string;
  updatedAt: string;
};

export type FolderWithCount = Folder & {
  memoCount: number;
};

export type FolderSelection = "root" | (string & {});

export type Memo = {
  id: string;
  vaultId: string;
  folderId: string | null;
  title: string;
  content: string;
  createdAt: string;
  updatedAt: string;
};

export type CreateMemoInput = {
  vaultId: string;
  folderId?: string | null;
  title: string;
  content: string;
};

export type UpdateMemoInput = {
  folderId?: string | null;
  title?: string;
  content?: string;
};

export type CreateFolderInput = {
  vaultId: string;
  parentId?: string | null;
  name: string;
};

export type UpdateFolderInput = {
  parentId?: string | null;
  name?: string;
};
```

**Verification**

```bash
npm test --workspace @memo/shared
npm run typecheck --workspace @memo/shared
```

## Task 04: folders APIの一覧と作成を実装する

**Goal**

Vault内フォルダをフラット配列で返し、親フォルダ作成時にVault一致を検証する。

**Files**

- `apps/api/src/index.ts`
- `apps/api/src/routes/folders.ts`
- `apps/api/src/routes/folders.test.ts`
- `apps/api/src/test/db.ts`

**Test**

```ts
// apps/api/src/routes/folders.test.ts
import { env } from "cloudflare:test";
import { describe, expect, test } from "vitest";
import "../test/db";
import { seedFolder, seedVault } from "../test/db";
import app from "../index";

describe("folders create and list", () => {
  test("returns folders in a vault with direct memoCount", async () => {
    await seedVault(env.DB, "v1");
    const create = await app.request("/api/folders", {
      method: "POST",
      body: JSON.stringify({ vaultId: "v1", name: "技術メモ" }),
      headers: { "Content-Type": "application/json" },
    }, env);
    expect(create.status).toBe(201);

    const list = await app.request("/api/folders?vaultId=v1", {}, env);
    expect(await list.json()).toMatchObject([
      { vaultId: "v1", parentId: null, name: "技術メモ", memoCount: 0 },
    ]);
  });

  test("rejects a parent folder from another vault", async () => {
    await seedVault(env.DB, "v1");
    await seedVault(env.DB, "v2");
    await seedFolder(env.DB, { id: "f2", vaultId: "v2", parentId: null, name: "外部" });

    const res = await app.request("/api/folders", {
      method: "POST",
      body: JSON.stringify({ vaultId: "v1", parentId: "f2", name: "子" }),
      headers: { "Content-Type": "application/json" },
    }, env);

    expect(res.status).toBe(422);
  });
});
```

**Implementation**

```ts
// apps/api/src/test/db.ts
export async function seedVault(db: D1Database, id: string) {
  const now = "2026-07-02T00:00:00.000Z";
  await db.prepare(
    "INSERT INTO vaults (id, name, created_at, updated_at) VALUES (?, ?, ?, ?)"
  ).bind(id, id, now, now).run();
}

export async function seedFolder(
  db: D1Database,
  input: { id: string; vaultId: string; parentId: string | null; name: string }
) {
  const now = "2026-07-02T00:00:00.000Z";
  await db.prepare(
    "INSERT INTO folders (id, vault_id, parent_id, name, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?)"
  ).bind(input.id, input.vaultId, input.parentId, input.name, now, now).run();
}
```

```ts
// apps/api/src/index.ts
import folders from "./routes/folders.js";

app.route("/api/folders", folders);
```

```ts
// apps/api/src/routes/folders.ts
async function validateParentInVault(
  db: ReturnType<typeof drizzle>,
  vaultId: string,
  parentId: string | null
) {
  if (parentId === null) return null;
  const [parent] = await db
    .select({ id: folders.id })
    .from(folders)
    .where(and(eq(folders.id, parentId), eq(folders.vaultId, vaultId)));
  return parent ? null : "Parent folder does not belong to vault";
}

app.get("/", async (c) => {
  const db = drizzle(c.env.DB);
  const vaultId = c.req.query("vaultId");
  if (!vaultId) return c.json({ error: "vaultId is required" }, 400);

  const result = await db
    .select({
      id: folders.id,
      vaultId: folders.vaultId,
      parentId: folders.parentId,
      name: folders.name,
      createdAt: folders.createdAt,
      updatedAt: folders.updatedAt,
      memoCount: count(memos.id),
    })
    .from(folders)
    .leftJoin(memos, eq(memos.folderId, folders.id))
    .where(eq(folders.vaultId, vaultId))
    .groupBy(folders.id)
    .orderBy(folders.name);
  return c.json(result);
});

app.post("/", async (c) => {
  const db = drizzle(c.env.DB);
  const body = await c.req.json<{ vaultId: string; parentId?: string | null; name: string }>();
  const parentId = body.parentId ?? null;
  const parentError = await validateParentInVault(db, body.vaultId, parentId);
  if (parentError) return c.json({ error: parentError }, 422);

  const now = new Date().toISOString();
  const folder = {
    id: crypto.randomUUID(),
    vaultId: body.vaultId,
    parentId,
    name: body.name,
    createdAt: now,
    updatedAt: now,
  };
  await db.insert(folders).values(folder);
  return c.json(folder, 201);
});
```

**Verification**

```bash
npm test --workspace @memo/api -- src/routes/folders.test.ts
npm run typecheck --workspace @memo/api
```

## Task 05: folders APIのリネームと移動を実装する

**Goal**

フォルダ名変更と親変更を実装し、同一Vault外の親とサイクルを422で拒否する。

**Files**

- `apps/api/src/routes/folders.ts`
- `apps/api/src/routes/folders.test.ts`

**Test**

```ts
// apps/api/src/routes/folders.test.ts
test("rejects moving a folder under its descendant", async () => {
  await seedVault(env.DB, "v1");
  await seedFolder(env.DB, { id: "a", vaultId: "v1", parentId: null, name: "A" });
  await seedFolder(env.DB, { id: "b", vaultId: "v1", parentId: "a", name: "B" });

  const res = await app.request("/api/folders/a", {
    method: "PUT",
    body: JSON.stringify({ parentId: "b" }),
    headers: { "Content-Type": "application/json" },
  }, env);

  expect(res.status).toBe(422);
  expect(await res.json()).toEqual({ error: "Folder cycle is not allowed" });
});

test("rejects moving a folder under a parent from another vault", async () => {
  await seedVault(env.DB, "v1");
  await seedVault(env.DB, "v2");
  await seedFolder(env.DB, { id: "a", vaultId: "v1", parentId: null, name: "A" });
  await seedFolder(env.DB, { id: "external", vaultId: "v2", parentId: null, name: "外部" });

  const res = await app.request("/api/folders/a", {
    method: "PUT",
    body: JSON.stringify({ parentId: "external" }),
    headers: { "Content-Type": "application/json" },
  }, env);

  expect(res.status).toBe(422);
  expect(await res.json()).toEqual({ error: "Parent folder does not belong to vault" });
});
```

**Implementation**

```ts
// apps/api/src/routes/folders.ts
async function wouldCreateCycle(
  db: ReturnType<typeof drizzle>,
  folderId: string,
  nextParentId: string | null
) {
  let current = nextParentId;
  while (current) {
    if (current === folderId) return true;
    const [parent] = await db
      .select({ parentId: folders.parentId })
      .from(folders)
      .where(eq(folders.id, current));
    current = parent?.parentId ?? null;
  }
  return false;
}

app.put("/:id", async (c) => {
  const db = drizzle(c.env.DB);
  const folderId = c.req.param("id");
  const body = await c.req.json<{ name?: string; parentId?: string | null }>();

  const [current] = await db
    .select({ id: folders.id, vaultId: folders.vaultId })
    .from(folders)
    .where(eq(folders.id, folderId));
  if (!current) return c.json({ error: "Not found" }, 404);

  if (body.parentId !== undefined) {
    const parentError = await validateParentInVault(db, current.vaultId, body.parentId);
    if (parentError) return c.json({ error: parentError }, 422);
  }

  const createsCycle =
    body.parentId !== undefined &&
    (await wouldCreateCycle(db, folderId, body.parentId));
  if (createsCycle) {
    return c.json({ error: "Folder cycle is not allowed" }, 422);
  }

  const updates: { name?: string; parentId?: string | null; updatedAt: string } = {
    updatedAt: new Date().toISOString(),
  };
  if (body.name !== undefined) updates.name = body.name;
  if (body.parentId !== undefined) updates.parentId = body.parentId;

  const [updated] = await db
    .update(folders)
    .set(updates)
    .where(eq(folders.id, folderId))
    .returning();
  return c.json(updated);
});
```

**Verification**

```bash
npm test --workspace @memo/api -- src/routes/folders.test.ts
npm run typecheck --workspace @memo/api
```

## Task 06: folders APIの空フォルダ削除を実装する

**Goal**

子フォルダまたは直下メモを持つフォルダの削除を409で拒否する。

**Files**

- `apps/api/src/routes/folders.ts`
- `apps/api/src/routes/folders.test.ts`
- `apps/api/src/test/db.ts`

**Test**

```ts
// apps/api/src/routes/folders.test.ts
test("returns 409 when deleting a folder with a child folder", async () => {
  await seedVault(env.DB, "v1");
  await seedFolder(env.DB, { id: "a", vaultId: "v1", parentId: null, name: "A" });
  await seedFolder(env.DB, { id: "b", vaultId: "v1", parentId: "a", name: "B" });

  const res = await app.request("/api/folders/a", { method: "DELETE" }, env);

  expect(res.status).toBe(409);
  expect(await res.json()).toEqual({ error: "Folder is not empty" });
});
```

**Implementation**

```ts
// apps/api/src/routes/folders.ts
async function folderIsEmpty(db: ReturnType<typeof drizzle>, folderId: string) {
  const [children] = await db
    .select({ value: count() })
    .from(folders)
    .where(eq(folders.parentId, folderId));
  const [assignedMemos] = await db
    .select({ value: count() })
    .from(memos)
    .where(eq(memos.folderId, folderId));
  return children.value === 0 && assignedMemos.value === 0;
}

app.delete("/:id", async (c) => {
  const db = drizzle(c.env.DB);
  const id = c.req.param("id");
  if (!(await folderIsEmpty(db, id))) {
    return c.json({ error: "Folder is not empty" }, 409);
  }
});
```

**Verification**

```bash
npm test --workspace @memo/api -- src/routes/folders.test.ts
npm run typecheck --workspace @memo/api
```

## Task 07: memos APIのfolderId検索を実装する

**Goal**

`folderId=root` はVault直下、通常の `folderId` はそのフォルダ直下、`q` 検索はVault全体を返す。

**Files**

- `apps/api/src/routes/memos.ts`
- `apps/api/src/routes/memos.test.ts`
- `apps/api/src/test/db.ts`

**Test**

```ts
// apps/api/src/routes/memos.test.ts
import { env } from "cloudflare:test";
import { describe, expect, test } from "vitest";
import "../test/db";
import { seedFolder, seedMemo, seedVault } from "../test/db";
import app from "../index";

test("filters by root, direct folder, and vault-wide search", async () => {
  await seedVault(env.DB, "v1");
  await seedFolder(env.DB, { id: "f1", vaultId: "v1", parentId: null, name: "技術メモ" });
  await seedMemo(env.DB, { id: "rootMemo", vaultId: "v1", folderId: null, title: "Root", content: "" });
  await seedMemo(env.DB, { id: "folderMemo", vaultId: "v1", folderId: "f1", title: "Cloudflare", content: "" });

  const root = await app.request("/api/memos?vaultId=v1&folderId=root", {}, env);
  expect(await root.json()).toMatchObject([{ id: "rootMemo" }]);

  const direct = await app.request("/api/memos?vaultId=v1&folderId=f1", {}, env);
  expect(await direct.json()).toMatchObject([{ id: "folderMemo" }]);

  const search = await app.request("/api/memos?vaultId=v1&folderId=root&q=Cloudflare", {}, env);
  expect(await search.json()).toMatchObject([{ id: "folderMemo" }]);
});
```

**Implementation**

```ts
// apps/api/src/test/db.ts
export async function seedMemo(
  db: D1Database,
  input: { id: string; vaultId: string; folderId: string | null; title: string; content: string }
) {
  const now = "2026-07-02T00:00:00.000Z";
  await db.prepare(
    "INSERT INTO memos (id, vault_id, folder_id, title, content, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?)"
  ).bind(input.id, input.vaultId, input.folderId, input.title, input.content, now, now).run();
}
```

```ts
// apps/api/src/routes/memos.ts
const folderId = c.req.query("folderId");

if (search) {
  const pattern = `%${search}%`;
  const searchCondition = or(like(memos.title, pattern), like(memos.content, pattern));
  if (searchCondition) conditions.push(searchCondition);
} else if (folderId === "root") {
  conditions.push(isNull(memos.folderId));
} else if (folderId) {
  conditions.push(eq(memos.folderId, folderId));
}
```

**Verification**

```bash
npm test --workspace @memo/api -- src/routes/memos.test.ts
npm run typecheck --workspace @memo/api
```

## Task 08: memos APIのfolderId作成と移動を実装する

**Goal**

メモ作成と更新で `folderId` を保存し、メモとフォルダのVault不一致を422で拒否する。

**Files**

- `apps/api/src/routes/memos.ts`
- `apps/api/src/routes/memos.test.ts`
- `apps/api/src/test/db.ts`

**Test**

```ts
// apps/api/src/routes/memos.test.ts
import { env } from "cloudflare:test";
import { describe, expect, test } from "vitest";
import "../test/db";
import { seedFolder, seedVault } from "../test/db";
import app from "../index";

test("rejects assigning a memo to a folder from another vault", async () => {
  await seedVault(env.DB, "v1");
  await seedVault(env.DB, "v2");
  await seedFolder(env.DB, { id: "f2", vaultId: "v2", parentId: null, name: "外部" });

  const res = await app.request("/api/memos", {
    method: "POST",
    body: JSON.stringify({ vaultId: "v1", folderId: "f2", title: "x", content: "" }),
    headers: { "Content-Type": "application/json" },
  }, env);

  expect(res.status).toBe(422);
  expect(await res.json()).toEqual({ error: "Folder does not belong to the memo vault" });
});
```

**Implementation**

```ts
// apps/api/src/routes/memos.ts
async function validateMemoFolder(
  db: ReturnType<typeof drizzle>,
  vaultId: string,
  folderId: string | null | undefined
) {
  if (folderId == null) return true;
  const [folder] = await db
    .select({ id: folders.id })
    .from(folders)
    .where(and(eq(folders.id, folderId), eq(folders.vaultId, vaultId)));
  return Boolean(folder);
}

if (!(await validateMemoFolder(db, body.vaultId, body.folderId))) {
  return c.json({ error: "Folder does not belong to the memo vault" }, 422);
}
```

**Verification**

```bash
npm test --workspace @memo/api -- src/routes/memos.test.ts
npm run typecheck --workspace @memo/api
```

## Task 09: Web APIクライアントへfoldersとfolderIdを追加する

**Goal**

React側がfolders APIとmemoのfolderId入出力を型付きで呼べるようにする。

**Files**

- `apps/web/src/api.ts`
- `apps/web/src/api.test.ts`

**Test**

```ts
// apps/web/src/api.test.ts
import { afterEach, describe, expect, test, vi } from "vitest";
import { api } from "./api";

describe("api client", () => {
  afterEach(() => vi.restoreAllMocks());

  test("sends folderId=root for root memo listing", async () => {
    vi.spyOn(globalThis, "fetch").mockResolvedValue(
      new Response("[]", { status: 200, headers: { "Content-Type": "application/json" } })
    );

    await api.memos.list({ vaultId: "v1", folderId: "root" });

    expect(fetch).toHaveBeenCalledWith("/api/memos?vaultId=v1&folderId=root", expect.any(Object));
  });
});
```

**Implementation**

```ts
// apps/web/src/api.ts
folders: {
  list: (vaultId: string) => request<FolderWithCount[]>(`/folders?vaultId=${encodeURIComponent(vaultId)}`),
  create: (data: { vaultId: string; parentId?: string | null; name: string }) =>
    request<Folder>("/folders", { method: "POST", body: JSON.stringify(data) }),
  update: (id: string, data: { name?: string; parentId?: string | null }) =>
    request<Folder>(`/folders/${id}`, { method: "PUT", body: JSON.stringify(data) }),
  delete: (id: string) => request<{ ok: boolean }>(`/folders/${id}`, { method: "DELETE" }),
},
memos: {
  list: (params?: { vaultId?: string; folderId?: FolderSelection; q?: string }) => {
    const sp = new URLSearchParams();
    if (params?.vaultId) sp.set("vaultId", params.vaultId);
    if (params?.folderId) sp.set("folderId", params.folderId);
    if (params?.q) sp.set("q", params.q);
    const qs = sp.toString();
    return request<Memo[]>(`/memos${qs ? `?${qs}` : ""}`);
  },
}
```

**Verification**

```bash
npm test --workspace @memo/web -- src/api.test.ts
npm run typecheck --workspace @memo/web
```

## Task 10: フォルダ木の投影ユーティリティを追加する

**Goal**

フラット配列からツリー、サブツリー合計件数、パンくず、子孫ID集合を作る。

**Files**

- `apps/web/src/folders.ts`
- `apps/web/src/folders.test.ts`
- `apps/web/src/test/factories.ts`

**Test**

```ts
// apps/web/src/folders.test.ts
import { describe, expect, test } from "vitest";
import { buildFolderTree, getDescendantIds, getFolderPath } from "./folders";
import { folder } from "./test/factories";

test("builds total memo counts and paths", () => {
  const folders = [
    folder({ id: "a", parentId: null, name: "技術メモ", memoCount: 2 }),
    folder({ id: "b", parentId: "a", name: "Cloudflare", memoCount: 3 }),
  ];

  const tree = buildFolderTree(folders);

  expect(tree[0].totalMemoCount).toBe(5);
  expect(getFolderPath(folders, "b").map((f) => f.name)).toEqual(["技術メモ", "Cloudflare"]);
  expect(getDescendantIds(folders, "a")).toEqual(new Set(["b"]));
});
```

**Implementation**

```ts
// apps/web/src/folders.ts
export type FolderNode = FolderWithCount & {
  children: FolderNode[];
  depth: number;
  totalMemoCount: number;
};

export function buildFolderTree(folders: FolderWithCount[]): FolderNode[] {
  const byId = new Map<string, FolderNode>();
  for (const folder of folders) {
    byId.set(folder.id, {
      id: folder.id,
      vaultId: folder.vaultId,
      parentId: folder.parentId,
      name: folder.name,
      createdAt: folder.createdAt,
      updatedAt: folder.updatedAt,
      memoCount: folder.memoCount,
      children: [],
      depth: 0,
      totalMemoCount: folder.memoCount,
    });
  }
  const roots: FolderNode[] = [];
  for (const node of byId.values()) {
    if (node.parentId && byId.has(node.parentId)) byId.get(node.parentId)!.children.push(node);
    if (!node.parentId || !byId.has(node.parentId)) roots.push(node);
  }
  const visit = (node: FolderNode, depth: number): number => {
    node.depth = depth;
    node.children.sort((a, b) => a.name.localeCompare(b.name, "ja"));
    node.totalMemoCount = node.memoCount + node.children.reduce((sum, child) => sum + visit(child, depth + 1), 0);
    return node.totalMemoCount;
  };
  roots.sort((a, b) => a.name.localeCompare(b.name, "ja"));
  roots.forEach((node) => visit(node, 0));
  return roots;
}

export function getFolderPath(folders: FolderWithCount[], folderId: string) {
  const byId = new Map(folders.map((folder) => [folder.id, folder]));
  const path: FolderWithCount[] = [];
  let current = byId.get(folderId);
  while (current) {
    path.unshift(current);
    current = current.parentId ? byId.get(current.parentId) : undefined;
  }
  return path;
}

export function getDescendantIds(folders: FolderWithCount[], folderId: string) {
  const childrenByParent = new Map<string, FolderWithCount[]>();
  for (const folder of folders) {
    if (!folder.parentId) continue;
    const siblings = childrenByParent.get(folder.parentId) ?? [];
    siblings.push(folder);
    childrenByParent.set(folder.parentId, siblings);
  }
  const descendants = new Set<string>();
  const visit = (id: string) => {
    for (const child of childrenByParent.get(id) ?? []) {
      descendants.add(child.id);
      visit(child.id);
    }
  };
  visit(folderId);
  return descendants;
}
```

**Verification**

```bash
npm test --workspace @memo/web -- src/folders.test.ts
npm run typecheck --workspace @memo/web
```

## Task 11: AppのVault/Folder状態管理を組み替える

**Goal**

最後に開いたVault、Vaultごとの選択フォルダ、展開フォルダ、選択メモをlocalStorageへ保存し、Vault切り替え時に復元する。

**Files**

- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/test/factories.ts`
- `apps/web/src/test/mockApi.ts`

**Test**

```tsx
// apps/web/src/App.test.tsx
import { render, waitFor } from "@testing-library/react";
import { describe, expect, test, vi } from "vitest";
import { folder, vault } from "./test/factories";
import { mockApi } from "./test/mockApi";

vi.mock("./api", async () => ({
  api: (await import("./test/mockApi")).mockApi,
}));

import { App } from "./App";

test("loads folders and root memos for the restored vault", async () => {
  localStorage.setItem("memo:lastVaultId", "v1");
  localStorage.setItem("memo:vault:v1:folderId", "root");
  localStorage.setItem("memo:vault:v1:expandedFolderIds", JSON.stringify(["f1"]));
  mockApi.vaults.list.mockResolvedValue([vault("v1", "日々の記録", 3)]);
  mockApi.folders.list.mockResolvedValue([folder({ id: "f1", name: "技術メモ", memoCount: 2 })]);
  mockApi.memos.list.mockResolvedValue([]);

  render(<App />);

  await waitFor(() => expect(mockApi.folders.list).toHaveBeenCalledWith("v1"));
  expect(mockApi.memos.list).toHaveBeenCalledWith({ vaultId: "v1", folderId: "root" });
});
```

**Implementation**

```tsx
// apps/web/src/App.tsx
const [folders, setFolders] = useState<FolderWithCount[]>([]);
const [selectedFolderId, setSelectedFolderId] = useState<FolderSelection>("root");
const [expandedFolderIds, setExpandedFolderIds] = useState<Set<string>>(new Set());
const rootMemoCount = Math.max(
  (selectedVault?.memoCount ?? 0) -
    folders.reduce((sum, folder) => sum + folder.memoCount, 0),
  0
);

useEffect(() => {
  if (!selectedVault) return;
  localStorage.setItem("memo:lastVaultId", selectedVault.id);
  api.folders.list(selectedVault.id).then((loadedFolders) => {
    setFolders(loadedFolders);
    const savedFolderId =
      localStorage.getItem(`memo:vault:${selectedVault.id}:folderId`) ?? "root";
    const restoredFolderId =
      savedFolderId === "root" || loadedFolders.some((folder) => folder.id === savedFolderId)
        ? savedFolderId
        : "root";
    setSelectedFolderId(restoredFolderId);
    const validFolderIds = new Set(loadedFolders.map((folder) => folder.id));
    const savedExpandedIds = JSON.parse(
      localStorage.getItem(`memo:vault:${selectedVault.id}:expandedFolderIds`) ?? "[]"
    ) as string[];
    setExpandedFolderIds(
      new Set(savedExpandedIds.filter((folderId) => validFolderIds.has(folderId)))
    );
  });
  setSelectedMemo(null);
}, [selectedVault]);

const handleToggleFolder = (folderId: string) => {
  setExpandedFolderIds((current) => {
    const next = new Set(current);
    if (next.has(folderId)) next.delete(folderId);
    else next.add(folderId);
    return next;
  });
};

useEffect(() => {
  if (!selectedVault) return;
  localStorage.setItem(
    `memo:vault:${selectedVault.id}:expandedFolderIds`,
    JSON.stringify(Array.from(expandedFolderIds))
  );
}, [selectedVault, expandedFolderIds]);

useEffect(() => {
  if (!selectedVault) return;
  const params = search
    ? { vaultId: selectedVault.id, q: search }
    : { vaultId: selectedVault.id, folderId: selectedFolderId };
  api.memos.list(params).then(setMemos);
}, [selectedVault, selectedFolderId, search]);
```

**Verification**

```bash
npm test --workspace @memo/web -- src/App.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 12a: フォルダツリーの表示・開閉・選択を実装する

**Goal**

フォルダのみのツリー、展開ボタン、選択状態、サブツリー合計件数、`(未分類)` 行を描画する。

**Files**

- `apps/web/src/components/FolderTree.tsx`
- `apps/web/src/components/FolderTree.test.tsx`
- `apps/web/src/test/factories.ts`

**Test**

```tsx
// apps/web/src/components/FolderTree.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { node } from "../test/factories";
import { FolderTree } from "./FolderTree";

test("renders folders, toggles a branch, and selects uncategorized", async () => {
  const user = userEvent.setup();
  const onToggle = vi.fn();
  const onSelect = vi.fn();
  const parent = node({ id: "a", name: "技術メモ", totalMemoCount: 5 });
  const child = node({ id: "b", name: "Cloudflare", totalMemoCount: 3 });
  parent.children = [child];

  render(
    <FolderTree
      nodes={[parent]}
      selectedFolderId="root"
      expandedFolderIds={new Set(["a"])}
      rootMemoCount={2}
      onSelect={onSelect}
      onToggle={onToggle}
    />
  );

  await user.click(screen.getByRole("button", { name: "技術メモを開閉" }));
  await user.click(screen.getByRole("button", { name: "(未分類) 2" }));

  expect(screen.getByRole("button", { name: "Cloudflare 3" })).toBeInTheDocument();
  expect(onToggle).toHaveBeenCalledWith("a");
  expect(onSelect).toHaveBeenCalledWith("root");
});
```

**Implementation**

```tsx
// apps/web/src/components/FolderTree.tsx
export function FolderTree({
  nodes,
  selectedFolderId,
  expandedFolderIds,
  rootMemoCount,
  onSelect,
  onToggle,
}: Props) {
  const renderNode = (node: FolderNode) => {
    const expanded = expandedFolderIds.has(node.id);
    return (
      <div key={node.id}>
        <div className={node.id === selectedFolderId ? "text-lamp" : "text-fg-dim"}>
          <button aria-label={`${node.name}を開閉`} onClick={() => onToggle(node.id)}>
            {expanded ? "▾" : "▸"}
          </button>
          <button aria-label={`${node.name} ${node.totalMemoCount}`} onClick={() => onSelect(node.id)}>
            <span>{node.name}</span>
            <span>{node.totalMemoCount}</span>
          </button>
        </div>
        {expanded && node.children.map(renderNode)}
      </div>
    );
  };

  return (
    <nav className="min-h-0 flex-1 overflow-y-auto py-2">
      {nodes.map(renderNode)}
      <button aria-label={`(未分類) ${rootMemoCount}`} onClick={() => onSelect("root")}>
        <span>(未分類)</span>
        <span>{rootMemoCount}</span>
      </button>
    </nav>
  );
}
```

**Verification**

```bash
npm test --workspace @memo/web -- src/components/FolderTree.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 12b: フォルダ操作UIを追加する

**Goal**

フォルダ作成、リネーム、空フォルダ削除、親変更ピッカーを追加し、親変更ピッカーでは自身と子孫を無効にする。

**Files**

- `apps/web/src/components/FolderTree.tsx`
- `apps/web/src/components/FolderParentPicker.tsx`
- `apps/web/src/components/FolderTreeActions.test.tsx`
- `apps/web/src/folders.ts`
- `apps/web/src/test/factories.ts`

**Test**

```tsx
// apps/web/src/components/FolderTreeActions.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { folder, node } from "../test/factories";
import { FolderParentPicker } from "./FolderParentPicker";
import { FolderTree } from "./FolderTree";

test("creates a child folder from a row action", async () => {
  const user = userEvent.setup();
  const onCreateChild = vi.fn();
  render(
    <FolderTree
      nodes={[node({ id: "a", name: "技術メモ", totalMemoCount: 0 })]}
      selectedFolderId="a"
      expandedFolderIds={new Set()}
      rootMemoCount={0}
      onSelect={vi.fn()}
      onToggle={vi.fn()}
      onCreateChild={onCreateChild}
    />
  );

  await user.click(screen.getByRole("button", { name: "技術メモにフォルダを追加" }));
  await user.type(screen.getByRole("textbox", { name: "新しいフォルダ名" }), "Cloudflare");
  await user.keyboard("{Enter}");

  expect(onCreateChild).toHaveBeenCalledWith("a", "Cloudflare");
});

test("renames and deletes a folder from row actions", async () => {
  const user = userEvent.setup();
  const onRename = vi.fn();
  const onDelete = vi.fn();
  render(
    <FolderTree
      nodes={[node({ id: "a", name: "技術メモ", totalMemoCount: 0 })]}
      selectedFolderId="a"
      expandedFolderIds={new Set()}
      rootMemoCount={0}
      onSelect={vi.fn()}
      onToggle={vi.fn()}
      onRename={onRename}
      onDelete={onDelete}
    />
  );

  await user.click(screen.getByRole("button", { name: "技術メモをリネーム" }));
  await user.clear(screen.getByRole("textbox", { name: "フォルダ名" }));
  await user.type(screen.getByRole("textbox", { name: "フォルダ名" }), "技術ログ");
  await user.keyboard("{Enter}");
  await user.click(screen.getByRole("button", { name: "技術メモを削除" }));

  expect(onRename).toHaveBeenCalledWith("a", "技術ログ");
  expect(onDelete).toHaveBeenCalledWith("a");
});

test("disables the moving folder and descendants in parent picker", () => {
  const folders = [
    folder({ id: "a", parentId: null, name: "技術メモ", memoCount: 0 }),
    folder({ id: "b", parentId: "a", name: "Cloudflare", memoCount: 0 }),
  ];

  render(<FolderParentPicker folders={folders} folderId="a" value={null} onChange={vi.fn()} />);

  expect(screen.getByRole("option", { name: "Vault直下" })).toBeEnabled();
  expect(screen.getByRole("option", { name: "技術メモ" })).toBeDisabled();
  expect(screen.getByRole("option", { name: "Cloudflare" })).toBeDisabled();
});
```

**Implementation**

```tsx
// apps/web/src/components/FolderParentPicker.tsx
export function FolderParentPicker({ folders, folderId, value, onChange }: Props) {
  const disabledIds = getDescendantIds(folders, folderId);
  disabledIds.add(folderId);
  return (
    <select aria-label="親フォルダ" value={value ?? "root"} onChange={(event) => onChange(event.target.value === "root" ? null : event.target.value)}>
      <option value="root">Vault直下</option>
      {folders.map((folder) => (
        <option key={folder.id} value={folder.id} disabled={disabledIds.has(folder.id)}>
          {folder.name}
        </option>
      ))}
    </select>
  );
}
```

```tsx
// apps/web/src/components/FolderTree.tsx
<button aria-label={`${node.name}にフォルダを追加`} onClick={() => setCreatingParentId(node.id)}>
  +
</button>
<button aria-label={`${node.name}をリネーム`} onClick={() => setRenamingFolderId(node.id)}>
  rename
</button>
<button aria-label={`${node.name}を削除`} onClick={() => onDelete?.(node.id)}>
  delete
</button>
<button aria-label={`${node.name}を移動`} onClick={() => setMovingFolderId(node.id)}>
  move
</button>
{creatingParentId === node.id && (
  <form onSubmit={(event) => { event.preventDefault(); onCreateChild?.(node.id, creatingName.trim()); }}>
    <input aria-label="新しいフォルダ名" value={creatingName} onChange={(event) => setCreatingName(event.target.value)} />
  </form>
)}
{renamingFolderId === node.id && (
  <form onSubmit={(event) => { event.preventDefault(); onRename?.(node.id, renamingName.trim()); }}>
    <input aria-label="フォルダ名" value={renamingName} onChange={(event) => setRenamingName(event.target.value)} />
  </form>
)}
```

**Verification**

```bash
npm test --workspace @memo/web -- src/components/FolderTreeActions.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 13: Vaultスイッチャーを追加する

**Goal**

常駐Vault一覧を廃止し、左レール上部のカードからVaultを切り替える。

**Files**

- `apps/web/src/components/VaultSwitcher.tsx`
- `apps/web/src/components/VaultSwitcher.test.tsx`
- `apps/web/src/test/factories.ts`

**Test**

```tsx
// apps/web/src/components/VaultSwitcher.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { vault } from "../test/factories";
import { VaultSwitcher } from "./VaultSwitcher";

test("opens vault menu and selects another vault", async () => {
  const user = userEvent.setup();
  const onSelect = vi.fn();
  render(
    <VaultSwitcher
      vaults={[vault("v1", "日々の記録", 3), vault("v2", "仕事", 1)]}
      selectedVault={vault("v1", "日々の記録", 3)}
      onSelect={onSelect}
      onCreate={vi.fn()}
    />
  );

  await user.click(screen.getByRole("button", { name: "Vaultを切り替える" }));
  await user.click(screen.getByRole("menuitem", { name: "仕事 1" }));

  expect(onSelect).toHaveBeenCalledWith(expect.objectContaining({ id: "v2" }));
});
```

**Implementation**

```tsx
// apps/web/src/components/VaultSwitcher.tsx
export function VaultSwitcher({ vaults, selectedVault, onSelect, onCreate }: Props) {
  const [open, setOpen] = useState(false);
  return (
    <div className="relative">
      <button aria-label="Vaultを切り替える" onClick={() => setOpen((value) => !value)} className="flex w-full items-center gap-2 rounded-lg border border-line bg-night-raised px-3 py-2 text-left">
        <span className="h-1.5 w-1.5 rounded-full bg-lamp" />
        <span className="min-w-0 flex-1 truncate text-sm font-medium">{selectedVault?.name ?? "Vaultなし"}</span>
        <span className="text-xs text-fg-faint">⇄</span>
      </button>
      {open && <VaultMenu vaults={vaults} selectedVault={selectedVault} onSelect={onSelect} onCreate={onCreate} />}
    </div>
  );
}
```

**Verification**

```bash
npm test --workspace @memo/web -- src/components/VaultSwitcher.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 14: FolderRailをAppへ接続する

**Goal**

左ペインをVaultスイッチャー + 現Vaultのフォルダツリーへ置き換える。

**Files**

- `apps/web/src/App.tsx`
- `apps/web/src/components/FolderRail.tsx`
- `apps/web/src/components/FolderRail.test.tsx`
- `apps/web/src/components/VaultRail.tsx`
- `apps/web/src/test/factories.ts`

**Test**

```tsx
// apps/web/src/components/FolderRail.test.tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, test, vi } from "vitest";
import { node, vault } from "../test/factories";
import { FolderRail } from "./FolderRail";

test("renders switcher, folder tree, and uncategorized row", () => {
  render(
    <FolderRail
      vaults={[vault("v1", "日々の記録", 3)]}
      selectedVault={vault("v1", "日々の記録", 3)}
      folderNodes={[node({ id: "f1", name: "技術メモ", totalMemoCount: 2 })]}
      selectedFolderId="root"
      expandedFolderIds={new Set()}
      rootMemoCount={1}
      onSelectVault={vi.fn()}
      onCreateVault={vi.fn()}
      onSelectFolder={vi.fn()}
      onToggleFolder={vi.fn()}
      onCreateFolder={vi.fn()}
      onRenameFolder={vi.fn()}
      onDeleteFolder={vi.fn()}
      onMoveFolder={vi.fn()}
    />
  );

  expect(screen.getByText("日々の記録")).toBeInTheDocument();
  expect(screen.getByText("技術メモ")).toBeInTheDocument();
  expect(screen.getByText("(未分類)")).toBeInTheDocument();
});
```

**Implementation**

```tsx
// apps/web/src/App.tsx
<FolderRail
  vaults={vaults}
  selectedVault={selectedVault}
  folderNodes={folderNodes}
  selectedFolderId={selectedFolderId}
  expandedFolderIds={expandedFolderIds}
  rootMemoCount={rootMemoCount}
  onSelectVault={handleSelectVault}
  onCreateVault={handleCreateVault}
  onSelectFolder={handleSelectFolder}
  onToggleFolder={handleToggleFolder}
  onCreateFolder={handleCreateFolder}
  onRenameFolder={handleRenameFolder}
  onDeleteFolder={handleDeleteFolder}
  onMoveFolder={handleMoveFolder}
/>
```

```tsx
// apps/web/src/components/FolderRail.tsx
export function FolderRail(props: Props) {
  return (
    <aside className="hidden w-64 shrink-0 flex-col border-r border-line bg-night-deep/55 px-4 py-6 md:flex">
      <div className="mb-5 px-1 font-display text-xl font-semibold tracking-wide">memo<span className="text-lamp">.</span></div>
      <VaultSwitcher vaults={props.vaults} selectedVault={props.selectedVault} onSelect={props.onSelectVault} onCreate={props.onCreateVault} />
      <FolderTree nodes={props.folderNodes} selectedFolderId={props.selectedFolderId} expandedFolderIds={props.expandedFolderIds} rootMemoCount={props.rootMemoCount} onSelect={props.onSelectFolder} onToggle={props.onToggleFolder} onCreateChild={props.onCreateFolder} onRename={props.onRenameFolder} onDelete={props.onDeleteFolder} onMove={props.onMoveFolder} />
    </aside>
  );
}
```

**Verification**

```bash
npm test --workspace @memo/web -- src/components/FolderRail.test.tsx src/App.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 15: メモ一覧とエディタへフォルダ文脈を表示する

**Goal**

メモ一覧にVault名を含まないパンくずを出し、新規メモは選択フォルダ直下に作り、エディタメタ行からメモ移動ピッカーを開く。

**Files**

- `apps/web/src/App.tsx`
- `apps/web/src/components/MemoList.tsx`
- `apps/web/src/components/MemoBreadcrumb.tsx`
- `apps/web/src/components/MemoBreadcrumb.test.tsx`
- `apps/web/src/components/Editor.tsx`
- `apps/web/src/components/MemoFolderPicker.tsx`
- `apps/web/src/components/MemoFolderPicker.test.tsx`
- `apps/web/src/test/factories.ts`

**Test**

```tsx
// apps/web/src/components/MemoBreadcrumb.test.tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, test } from "vitest";
import { folder } from "../test/factories";
import { MemoBreadcrumb } from "./MemoBreadcrumb";

test("renders folder path without the vault name", () => {
  render(
    <MemoBreadcrumb
      path={[
        folder({ id: "a", name: "技術メモ" }),
        folder({ id: "b", parentId: "a", name: "Cloudflare" }),
      ]}
      vaultName="日々の記録"
    />
  );

  expect(screen.getByText("技術メモ / Cloudflare")).toBeInTheDocument();
  expect(screen.queryByText("日々の記録")).not.toBeInTheDocument();
});
```

```tsx
// apps/web/src/components/MemoFolderPicker.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { folder } from "../test/factories";
import { MemoFolderPicker } from "./MemoFolderPicker";

test("moves a memo to root with null folderId", async () => {
  const user = userEvent.setup();
  const onChange = vi.fn();
  render(<MemoFolderPicker folders={[folder({ id: "f1", name: "技術メモ" })]} value="f1" onChange={onChange} />);

  await user.selectOptions(screen.getByLabelText("メモの移動先"), "root");

  expect(onChange).toHaveBeenCalledWith(null);
});
```

**Implementation**

```tsx
// apps/web/src/components/MemoBreadcrumb.tsx
export function MemoBreadcrumb({ path }: { path: FolderWithCount[]; vaultName: string }) {
  return (
    <div className="breadcrumb px-3.5 pb-1 text-xs text-fg-faint">
      {path.length === 0 ? "(未分類)" : path.map((folder) => folder.name).join(" / ")}
    </div>
  );
}
```

```tsx
// apps/web/src/App.tsx
const selectedFolderPath =
  selectedFolderId === "root" ? [] : getFolderPath(folders, selectedFolderId);

const handleCreateMemo = async () => {
  if (!selectedVault) return;
  const memo = await api.memos.create({
    vaultId: selectedVault.id,
    folderId: selectedFolderId === "root" ? null : selectedFolderId,
    title: "",
    content: "",
  });
  setMemos((prev) => [memo].concat(prev));
  handleSelectMemo(memo);
};

const handleMoveMemo = async (folderId: string | null) => {
  if (!selectedMemo) return;
  const updated = await api.memos.update(selectedMemo.id, { folderId });
  const targetSelection = folderId ?? "root";
  setSelectedMemo(updated);
  setMemos((prev) =>
    targetSelection === selectedFolderId
      ? prev.map((memo) => (memo.id === updated.id ? updated : memo))
      : prev.filter((memo) => memo.id !== updated.id)
  );
  await api.folders.list(updated.vaultId).then(setFolders);
};
```

```tsx
// apps/web/src/App.tsx
<MemoBreadcrumb path={selectedFolderPath} vaultName={selectedVault?.name ?? ""} />
<MemoList
  memos={memos}
  selectedId={selectedMemo?.id ?? null}
  onSelect={handleSelectMemo}
/>
```

```tsx
// apps/web/src/components/Editor.tsx
<button onClick={() => setPickerOpen(true)} className="text-fg-faint hover:text-fg">
  {folderPath.length > 0 ? folderPath.join(" / ") : "(未分類)"}
</button>
```

**Verification**

```bash
npm test --workspace @memo/web -- src/components/MemoBreadcrumb.test.tsx src/components/MemoFolderPicker.test.tsx src/App.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 16: モバイルをツリー起点の3画面フローへ変更する

**Goal**

モバイルの初期画面をフォルダツリーにし、フォルダタップでメモ一覧、メモタップでエディタへ遷移する。

**Files**

- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/components/FolderRail.tsx`
- `apps/web/src/components/FolderTree.tsx`
- `apps/web/src/test/factories.ts`
- `apps/web/src/test/mockApi.ts`

**Test**

```tsx
// apps/web/src/App.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { folder, memo, vault } from "./test/factories";
import { mockApi } from "./test/mockApi";

vi.mock("./api", async () => ({
  api: (await import("./test/mockApi")).mockApi,
}));

import { App } from "./App";

test("mobile flow opens list from a folder and creates a memo in that folder", async () => {
  const user = userEvent.setup();
  mockApi.vaults.list.mockResolvedValue([vault("v1", "日々の記録", 0)]);
  mockApi.folders.list.mockResolvedValue([folder({ id: "f1", vaultId: "v1", name: "技術メモ", memoCount: 0 })]);
  mockApi.memos.list.mockResolvedValue([]);
  mockApi.memos.create.mockResolvedValue(memo({ id: "m1", vaultId: "v1", folderId: "f1" }));

  render(<App />);
  await user.click(await screen.findByRole("button", { name: "技術メモ" }));
  await user.click(screen.getByRole("button", { name: "新しいメモ" }));

  expect(mockApi.memos.create).toHaveBeenCalledWith({ vaultId: "v1", folderId: "f1", title: "", content: "" });
});
```

**Implementation**

```tsx
// apps/web/src/App.tsx
const [mobileView, setMobileView] = useState<"tree" | "list" | "editor">("tree");

const handleSelectFolder = (folderId: FolderSelection) => {
  setSelectedFolderId(folderId);
  if (selectedVault) localStorage.setItem(`memo:vault:${selectedVault.id}:folderId`, folderId);
  setSelectedMemo(null);
  setMobileView("list");
};

const handleSelectMemo = (memo: Memo) => {
  flushPendingSave();
  setSelectedMemo(memo);
  setTitle(memo.title);
  setContent(memo.content);
  setMobileView("editor");
};
```

**Verification**

```bash
npm test --workspace @memo/web -- src/App.test.tsx
npm run typecheck --workspace @memo/web
```

## Task 17: CLAUDE.mdのVault機能記述を更新する

**Goal**

リポジトリ説明を実態に合わせ、Vaultスイッチャー、任意深さフォルダ木、3ペイン構成を明記する。

**Files**

- `CLAUDE.md`

**Test**

```bash
rg -n "Vaultスイッチャー|任意深さのフォルダ木|3ペイン" CLAUDE.md
```

**Implementation**

```md
## Initial Scope

- メモの作成・編集・一覧・検索
- Vault機能 (常に1つのVaultを開き、Vaultスイッチャーで切り替える)
- フォルダ機能 (Vault内に任意深さのフォルダ木を持ち、3ペイン構成でフォルダツリー / メモ一覧 / エディタを表示する)
- PWA対応 (iPhoneからホーム画面に追加して利用)
```

**Verification**

```bash
rg -n "Vaultスイッチャー|任意深さのフォルダ木|3ペイン" CLAUDE.md
```

## Task 18: デプロイ前確認を固定する

**Goal**

テスト、型検査、ビルド、D1ローカルマイグレーション、Workers dry-runを同じ手順で確認する。

**Files**

- `package.json`
- `apps/api/package.json`

**Implementation**

```jsonc
// package.json
{
  "scripts": {
    "verify:folders": "npm test && npm run typecheck && npm run build"
  }
}
```

```jsonc
// apps/api/package.json
{
  "scripts": {
    "deploy:check": "wrangler deploy --dry-run --outdir dist"
  }
}
```

**Verification**

```bash
npm test
npm run typecheck
npm run build
npm run db:migrate:local --workspace @memo/api
npm run deploy:check --workspace @memo/api
npm run db:migrate:remote --workspace @memo/api
```
